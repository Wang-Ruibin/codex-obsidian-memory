Run the weekly project kickoff brief for the configured Obsidian memory vault.

Read the memory home, global memory, project index, and every project and branch page. Treat a project as active only when notes contain an explicit unfinished goal, blocker, next step, or recent durable progress. Do not treat placeholders as progress.

Update only the configured report's `updated` frontmatter date and latest-brief section with:

1. ISO week and generated time.
2. Five highest-value evidence-backed tasks, with project links, expected outcomes and priority rationale.
3. Current blockers, affected work and concrete unblock actions.
4. Historical notes needed this week, linked to exact decisions, branch pages or failure lessons.

If fewer than five real tasks are documented, do not invent work. Mark missing slots as priority awaiting confirmation and state what information is missing. Replace the prior brief instead of appending a transcript. Do not change project status to fit the brief.

Use Obsidian Wiki links for knowledge-cluster sources and ordinary relative Markdown links for project sources so the report does not create extra graph bridges. Verify links before finishing. Never store credentials.
