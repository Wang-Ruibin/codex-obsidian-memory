Run the monthly health audit for the configured Obsidian memory vault.

Inspect every non-hidden Markdown note. Find duplicated facts, stale claims, conflicts, broken links, orphan nodes, duplicate project homes, duplicate branch identities and project-index drift. Resolve only issues supported by clear evidence. Mark uncertain conflicts for review.

Create or replace the configured monthly audit with the month, scope, automatic fixes, unresolved conflicts, archive recommendations and graph statistics. Never delete, move or archive a file automatically; recommendations are labels only. Avoid adding pages solely to improve graph appearance. Never store credentials.
