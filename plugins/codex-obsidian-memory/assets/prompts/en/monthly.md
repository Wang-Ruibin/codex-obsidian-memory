Run the monthly health audit for the configured Obsidian memory vault.

Inspect every non-hidden Markdown note. Find duplicated facts, stale claims, conflicts, broken links, orphan nodes, duplicate project homes, duplicate branch identities and project-index drift. Resolve only issues supported by clear evidence. Mark uncertain conflicts for review.

Update only the configured report's `updated` frontmatter date and latest-audit section with the month, scope, automatic fixes, unresolved conflicts, archive recommendations and graph statistics. Update the maintenance note only when this audit produces a durable maintenance conclusion.

Never delete, move or archive a file automatically; recommendations are labels only. Avoid adding pages solely to improve graph appearance. Verify project-home counts, branch-page counts, broken links and orphan nodes before finishing. Never store credentials.
